# teleport-rs — Complete File Structure

## Rust Crate: `teleport` (workspace member)

```
crates/teleport/
├── Cargo.toml
├── src/
│   ├── lib.rs                  # Public API: #[remote], AppError, TeleportRouter, TeleportType
│   ├── router.rs               # TeleportRouter::new().state().mount() — collects procedures and builds Axum Router
│   ├── error.rs                # AppError<T>, TransportError (Rust side), IntoResponse impl
│   ├── auth.rs                 # Auth middleware, AuthedUser type, cookie extraction
│   ├── extractors.rs           # Axum extractors for teleport procedures
│   ├── json.rs                 # Serialization helpers (devalue-compatible if needed)
│   └── private.rs              # Internal: inventory::submit! macro, ProcedureRegistration
├── tests/
│   ├── router_test.rs          # Router generation tests
│   ├── error_test.rs           # AppError serialization tests
│   └── auth_test.rs            # Auth middleware tests
```

## Rust Crate: `teleport-macros` (proc macro crate)

```
crates/teleport-macros/
├── Cargo.toml
├── src/
│   ├── lib.rs                  # #[remote] proc macro entry point
│   ├── remote.rs               # #[remote(query/command/form)] attribute macro implementation
│   ├── teleport_type.rs        # #[derive(TeleportType)] derive macro
│   └── utils.rs                # Shared proc macro utilities (parsing, code gen helpers)
```

## Rust Crate: `teleport-build` (build-time TS generation)

```
crates/teleport-build/
├── Cargo.toml
├── src/
│   ├── lib.rs                  # generate(config) entry point
│   ├── config.rs               # Config struct (output_dir, naming, namespace_style, etc.)
│   ├── typescript.rs           # Specta → TypeScript type generation
│   ├── client.rs               # TS client function generation (rpc calls)
│   ├── errors.rs               # Error type generation (AppError, TransportError, RpcResult)
│   └── naming.rs               # snake_case → camelCase, namespace resolution
```

## NPM Package: `@teleport-rs/client`

```
packages/client/
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts                # Public API: rpc, configure, RpcResult, helpers
│   ├── rpc.ts                  # Core rpc() function (fetch wrapper)
│   ├── config.ts               # RpcConfig, configure(), default settings
│   ├── result.ts               # RpcResult type, isAppError(), isTransportError(), unwrap()
│   └── types.ts                # TransportError, RpcResult type definitions
├── dist/                       # Built output
└── README.md
```

## NPM Package: `@teleport-rs/vite`

```
packages/vite/
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts                # teleportVite() plugin
│   └── watcher.ts              # File watcher for generated bindings
├── dist/
└── README.md
```

## Example Project (Monorepo)

```
examples/sveltekit-demo/
├── Cargo.toml                   # Rust workspace root
├── package.json                 # npm workspace root
│
├── rust-server/
│   ├── Cargo.toml
│   ├── src/
│   │   ├── main.rs              # Axum server setup, TeleportRouter::mount()
│   │   ├── bin/
│   │   │   └── export.rs        # calls teleport_build::generate() — run via `cargo run --bin export`
│   │   ├── state.rs             # AppState (db, config)
│   │   ├── auth.rs              # Auth middleware, session verification
│   │   ├── errors.rs            # Procedure-specific error types
│   │   └── api/
│   │       ├── mod.rs
│   │       ├── users.rs         # #[remote(query)] procedures
│   │       ├── auth.rs          # #[remote(command)] login/logout
│   │       └── posts.rs         # #[remote(query/command/form)] procedures
│   └── bindings/                # gitignored; generated at build time
│
├── frontend/
│   ├── package.json
│   ├── svelte.config.js         # teleportVite plugin config
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── src/
│   │   ├── lib/
│   │   │   ├── api/
│   │   │   │   ├── generated/   # gitignored; generated by teleport-build
│   │   │   │   │   ├── types.ts
│   │   │   │   │   ├── client.ts
│   │   │   │   │   └── errors.ts
│   │   │   │   ├── config.ts     # configure({ baseUrl: ... })
│   │   │   │   └── index.ts      # barrel re-exports
│   │   │   └── server/
│   │   │       └── data.remote.ts # handwritten remote functions
│   │   └── routes/
│   │       ├── +layout.svelte
│   │       ├── +page.svelte
│   │       └── auth/
│   │           └── login/
│   │               └── +page.svelte
│   └── static/
│
├── .gitignore                   # bindings/ and generated/
├── .github/
│   └── workflows/
│       └── build.yml            # CI pipeline
└── README.md
```

## Workspace `Cargo.toml`

```toml
[workspace]
members = [
    "crates/teleport",
    "crates/teleport-macros",
    "crates/teleport-build",
]
resolver = "3"

[workspace.package]
edition = "2024"
rust-version = "1.91"

[workspace.dependencies]
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
specta = "2"
specta-typescript = "0.0.8"  # Must match Specta v2's API — check compatibility on upgrade
tokio = { version = "1", features = ["full"] }
inventory = "0.3"
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
```

## Workspace `package.json`

```json
{
  "name": "teleport-rs-monorepo",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/vite",
    "examples/sveltekit-demo/frontend"
  ]
}
```

## `.gitignore`

```
# Generated bindings — rebuild on checkout
rust-server/bindings/
frontend/src/lib/api/generated/

# Rust
target/

# Node
node_modules/
dist/
.svelte-kit/
```

## Key Dependencies

### Rust (`crates/teleport/Cargo.toml`)

```toml
[dependencies]
axum = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
specta = { workspace = true }
inventory = { workspace = true }
teleport-macros = { path = "../teleport-macros" }

[features]
default = []
debug-manifest = []
```

### Rust (`crates/teleport-build/Cargo.toml`)

```toml
[dependencies]
specta = { workspace = true }
specta-typescript = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
```

### TypeScript (`packages/client/package.json`)

```json
{
  "name": "@teleport-rs/client",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "qs": "^6.13.0"
  },
  "devDependencies": {
    "@types/qs": "^6.9.0",
    "typescript": "^5.5.0"
  }
}
```
